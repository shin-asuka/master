<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="TeacherPeRubricMapper">
    
    <sql id="teacherPeRubricColumns">teacher_pe_rubric.id AS id, teacher_pe_rubric.template_id AS template_id, teacher_pe_rubric.name AS name, teacher_pe_rubric.seq AS seq</sql>
    
    <sql id="teacherPeRubricWhere">
        <trim prefix="WHERE" prefixOverrides="AND">
            <if test="id != null">AND teacher_pe_rubric.id = #{id}</if>
            <if test="templateId != null">AND teacher_pe_rubric.template_id = #{templateId}</if>
            <if test="name != null">AND teacher_pe_rubric.name = #{name}</if>
            <if test="seq != null">AND teacher_pe_rubric.seq = #{seq}</if>
        </trim>
    </sql>
    
    <sql id="teacherPeRubricOrder">
        <if test="orderString != null">ORDER BY ${orderString}</if>
    </sql>
    
    <sql id="teacherPeRubricLimit">LIMIT #{startLine}, #{limitLine}</sql>
    
    <resultMap type="com.vipkid.trpm.entity.TeacherPeRubric" id="teacherPeRubricResultMap">
        <id column="id" property="id" />
        <result column="template_id" property="templateId" />
        <result column="name" property="name" />
        <result column="seq" property="seq" />
    </resultMap>
    
    <select id="findDao" resultMap="teacherPeRubricResultMap">
        SELECT
        <include refid="teacherPeRubricColumns" />
        FROM teacher_pe_rubric
        <include refid="teacherPeRubricWhere" />
    </select>
    
    <select id="countDao" resultType="int">
        SELECT COUNT(*) FROM teacher_pe_rubric
        <include refid="teacherPeRubricWhere" />
    </select>
    
    <select id="pageDao" resultMap="teacherPeRubricResultMap">
        SELECT
        <include refid="teacherPeRubricColumns" />
        FROM teacher_pe_rubric
        <include refid="teacherPeRubricWhere" />
        <include refid="teacherPeRubricOrder" />
        <include refid="teacherPeRubricLimit" />
    </select>
    
    <select id="limitDao" resultMap="teacherPeRubricResultMap">
        SELECT
        <include refid="teacherPeRubricColumns" />
        FROM teacher_pe_rubric
        <include refid="teacherPeRubricWhere" />
        <include refid="teacherPeRubricOrder" />
        <include refid="teacherPeRubricLimit" />
    </select>
    
    <insert id="saveDao" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO teacher_pe_rubric
        <trim prefix="(" prefixOverrides=",">
            <if test="id != null">,id</if>
            <if test="templateId != null">,template_id</if>
            <if test="name != null">,name</if>
            <if test="seq != null">,seq</if>
        </trim>
        ) VALUES
        <trim prefix="(" prefixOverrides=",">
            <if test="id != null">,#{id}</if>
            <if test="templateId != null">,#{templateId}</if>
            <if test="name != null">,#{name}</if>
            <if test="seq != null">,#{seq}</if>
        </trim>
        )
    </insert>
    
    <update id="updateDao">
        UPDATE teacher_pe_rubric
        <trim prefix="SET" prefixOverrides=",">
            <if test="templateId != null">,template_id = #{templateId}</if>
            <if test="name != null">,name = #{name}</if>
            <if test="seq != null">,seq = #{seq}</if>
        </trim>
        <trim prefix="WHERE" prefixOverrides="AND">AND id = #{id}</trim>
    </update>
    
    <delete id="deleteDao">
        DELETE FROM teacher_pe_rubric
        <trim prefix="WHERE" prefixOverrides="AND">AND id = #{id}</trim>
    </delete>
    
</mapper>
