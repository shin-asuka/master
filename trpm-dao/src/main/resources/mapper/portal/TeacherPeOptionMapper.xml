<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="TeacherPeOptionMapper">
    
    <sql id="teacherPeOptionColumns">teacher_pe_option.id AS id, teacher_pe_option.criteria_id AS criteria_id, teacher_pe_option.description AS description, teacher_pe_option.points AS points, teacher_pe_option.seq AS seq</sql>
    
    <sql id="teacherPeOptionWhere">
        <trim prefix="WHERE" prefixOverrides="AND">
            <if test="id != null">AND teacher_pe_option.id = #{id}</if>
            <if test="criteriaId != null">AND teacher_pe_option.criteria_id = #{criteriaId}</if>
            <if test="description != null">AND teacher_pe_option.description = #{description}</if>
            <if test="points != null">AND teacher_pe_option.points = #{points}</if>
            <if test="seq != null">AND teacher_pe_option.seq = #{seq}</if>
        </trim>
    </sql>
    
    <sql id="teacherPeOptionOrder">
        <if test="orderString != null">ORDER BY ${orderString}</if>
    </sql>
    
    <sql id="teacherPeOptionLimit">LIMIT #{startLine}, #{limitLine}</sql>
    
    <resultMap type="com.vipkid.trpm.entity.TeacherPeOption" id="teacherPeOptionResultMap">
        <id column="id" property="id" />
        <result column="criteria_id" property="criteriaId" />
        <result column="description" property="description" />
        <result column="points" property="points" />
        <result column="seq" property="seq" />
    </resultMap>
    
    <select id="findDao" resultMap="teacherPeOptionResultMap">
        SELECT
        <include refid="teacherPeOptionColumns" />
        FROM teacher_pe_option
        <include refid="teacherPeOptionWhere" />
    </select>
    
    <select id="countDao" resultType="int">
        SELECT COUNT(*) FROM teacher_pe_option
        <include refid="teacherPeOptionWhere" />
    </select>
    
    <select id="pageDao" resultMap="teacherPeOptionResultMap">
        SELECT
        <include refid="teacherPeOptionColumns" />
        FROM teacher_pe_option
        <include refid="teacherPeOptionWhere" />
        <include refid="teacherPeOptionOrder" />
        <include refid="teacherPeOptionLimit" />
    </select>
    
    <select id="limitDao" resultMap="teacherPeOptionResultMap">
        SELECT
        <include refid="teacherPeOptionColumns" />
        FROM teacher_pe_option
        <include refid="teacherPeOptionWhere" />
        <include refid="teacherPeOptionOrder" />
        <include refid="teacherPeOptionLimit" />
    </select>
    
    <insert id="saveDao" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO teacher_pe_option
        <trim prefix="(" prefixOverrides=",">
            <if test="id != null">,id</if>
            <if test="criteriaId != null">,criteria_id</if>
            <if test="description != null">,description</if>
            <if test="points != null">,points</if>
            <if test="seq != null">,seq</if>
        </trim>
        ) VALUES
        <trim prefix="(" prefixOverrides=",">
            <if test="id != null">,#{id}</if>
            <if test="criteriaId != null">,#{criteriaId}</if>
            <if test="description != null">,#{description}</if>
            <if test="points != null">,#{points}</if>
            <if test="seq != null">,#{seq}</if>
        </trim>
        )
    </insert>
    
    <update id="updateDao">
        UPDATE teacher_pe_option
        <trim prefix="SET" prefixOverrides=",">
            <if test="criteriaId != null">,criteria_id = #{criteriaId}</if>
            <if test="description != null">,description = #{description}</if>
            <if test="points != null">,points = #{points}</if>
            <if test="seq != null">,seq = #{seq}</if>
        </trim>
        <trim prefix="WHERE" prefixOverrides="AND">AND id = #{id}</trim>
    </update>
    
    <delete id="deleteDao">
        DELETE FROM teacher_pe_option
        <trim prefix="WHERE" prefixOverrides="AND">AND id = #{id}</trim>
    </delete>
    
</mapper>
