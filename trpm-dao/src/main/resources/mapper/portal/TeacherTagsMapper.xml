<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="TeacherTagsMapper">
    
    <sql id="teacherTagsColumns">teacher_tags.id AS id, teacher_tags.tag_id AS tag_id, teacher_tags.teacher_id AS teacher_id</sql>
    
    <sql id="teacherTagsWhere">
        <trim prefix="WHERE" prefixOverrides="AND">
            <if test="id != null">AND teacher_tags.id = #{id}</if>
            <if test="tagId != null">AND teacher_tags.tag_id = #{tagId}</if>
            <if test="teacherId != null">AND teacher_tags.teacher_id = #{teacherId}</if>
        </trim>
    </sql>
    
    <sql id="teacherTagsOrder">
        <if test="orderString != null">ORDER BY ${orderString}</if>
    </sql>
    
    <sql id="teacherTagsLimit">LIMIT #{startLine}, #{limitLine}</sql>
    
    <resultMap type="com.vipkid.trpm.entity.TeacherTags" id="teacherTagsResultMap">
        <id column="id" property="id" />
        <result column="tag_id" property="tagId" />
        <result column="teacher_id" property="teacherId" />
    </resultMap>
    
    <select id="findDao" resultMap="teacherTagsResultMap">
        SELECT
        <include refid="teacherTagsColumns" />
        FROM teacher_tags
        <include refid="teacherTagsWhere" />
    </select>
    
    <select id="countDao" resultType="int">
        SELECT COUNT(*) FROM teacher_tags
        <include refid="teacherTagsWhere" />
    </select>
    
    <select id="pageDao" resultMap="teacherTagsResultMap">
        SELECT
        <include refid="teacherTagsColumns" />
        FROM teacher_tags
        <include refid="teacherTagsWhere" />
        <include refid="teacherTagsOrder" />
        <include refid="teacherTagsLimit" />
    </select>
    
    <select id="limitDao" resultMap="teacherTagsResultMap">
        SELECT
        <include refid="teacherTagsColumns" />
        FROM teacher_tags
        <include refid="teacherTagsWhere" />
        <include refid="teacherTagsOrder" />
        <include refid="teacherTagsLimit" />
    </select>
    
    <insert id="saveDao" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO teacher_tags
        <trim prefix="(" prefixOverrides=",">
            <if test="id != null">,id</if>
            <if test="tagId != null">,tag_id</if>
            <if test="teacherId != null">,teacher_id</if>
        </trim>
        ) VALUES
        <trim prefix="(" prefixOverrides=",">
            <if test="id != null">,#{id}</if>
            <if test="tagId != null">,#{tagId}</if>
            <if test="teacherId != null">,#{teacherId}</if>
        </trim>
        )
    </insert>
    
    <update id="updateDao">
        UPDATE teacher_tags
        <trim prefix="SET" prefixOverrides=",">
            <if test="tagId != null">,tag_id = #{tagId}</if>
            <if test="teacherId != null">,teacher_id = #{teacherId}</if>
        </trim>
        <trim prefix="WHERE" prefixOverrides="AND">AND id = #{id}</trim>
    </update>
    
    <delete id="deleteDao">
        DELETE FROM teacher_tags
        <trim prefix="WHERE" prefixOverrides="AND">AND id = #{id}</trim>
    </delete>
    
</mapper>
