<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="TeacherPeSectionMapper">
    
    <sql id="teacherPeSectionColumns">teacher_pe_section.id AS id, teacher_pe_section.rubric_id AS rubric_id, teacher_pe_section.name AS name, teacher_pe_section.seq AS seq</sql>
    
    <sql id="teacherPeSectionWhere">
        <trim prefix="WHERE" prefixOverrides="AND">
            <if test="id != null">AND teacher_pe_section.id = #{id}</if>
            <if test="rubricId != null">AND teacher_pe_section.rubric_id = #{rubricId}</if>
            <if test="name != null">AND teacher_pe_section.name = #{name}</if>
            <if test="seq != null">AND teacher_pe_section.seq = #{seq}</if>
        </trim>
    </sql>
    
    <sql id="teacherPeSectionOrder">
        <if test="orderString != null">ORDER BY ${orderString}</if>
    </sql>
    
    <sql id="teacherPeSectionLimit">LIMIT #{startLine}, #{limitLine}</sql>
    
    <resultMap type="com.vipkid.trpm.entity.TeacherPeSection" id="teacherPeSectionResultMap">
        <id column="id" property="id" />
        <result column="rubric_id" property="rubricId" />
        <result column="name" property="name" />
        <result column="seq" property="seq" />
    </resultMap>
    
    <select id="findDao" resultMap="teacherPeSectionResultMap">
        SELECT
        <include refid="teacherPeSectionColumns" />
        FROM teacher_pe_section
        <include refid="teacherPeSectionWhere" />
    </select>
    
    <select id="countDao" resultType="int">
        SELECT COUNT(*) FROM teacher_pe_section
        <include refid="teacherPeSectionWhere" />
    </select>
    
    <select id="pageDao" resultMap="teacherPeSectionResultMap">
        SELECT
        <include refid="teacherPeSectionColumns" />
        FROM teacher_pe_section
        <include refid="teacherPeSectionWhere" />
        <include refid="teacherPeSectionOrder" />
        <include refid="teacherPeSectionLimit" />
    </select>
    
    <select id="limitDao" resultMap="teacherPeSectionResultMap">
        SELECT
        <include refid="teacherPeSectionColumns" />
        FROM teacher_pe_section
        <include refid="teacherPeSectionWhere" />
        <include refid="teacherPeSectionOrder" />
        <include refid="teacherPeSectionLimit" />
    </select>
    
    <insert id="saveDao" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO teacher_pe_section
        <trim prefix="(" prefixOverrides=",">
            <if test="id != null">,id</if>
            <if test="rubricId != null">,rubric_id</if>
            <if test="name != null">,name</if>
            <if test="seq != null">,seq</if>
        </trim>
        ) VALUES
        <trim prefix="(" prefixOverrides=",">
            <if test="id != null">,#{id}</if>
            <if test="rubricId != null">,#{rubricId}</if>
            <if test="name != null">,#{name}</if>
            <if test="seq != null">,#{seq}</if>
        </trim>
        )
    </insert>
    
    <update id="updateDao">
        UPDATE teacher_pe_section
        <trim prefix="SET" prefixOverrides=",">
            <if test="rubricId != null">,rubric_id = #{rubricId}</if>
            <if test="name != null">,name = #{name}</if>
            <if test="seq != null">,seq = #{seq}</if>
        </trim>
        <trim prefix="WHERE" prefixOverrides="AND">AND id = #{id}</trim>
    </update>
    
    <delete id="deleteDao">
        DELETE FROM teacher_pe_section
        <trim prefix="WHERE" prefixOverrides="AND">AND id = #{id}</trim>
    </delete>
    
</mapper>
