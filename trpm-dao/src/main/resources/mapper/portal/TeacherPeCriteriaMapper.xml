<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="TeacherPeCriteriaMapper">
    
    <sql id="teacherPeCriteriaColumns">teacher_pe_criteria.id AS id, teacher_pe_criteria.section_id AS section_id, teacher_pe_criteria.title AS title, teacher_pe_criteria.type AS type, teacher_pe_criteria.points AS points, teacher_pe_criteria.calculated AS calculated, teacher_pe_criteria.seq AS seq</sql>
    
    <sql id="teacherPeCriteriaWhere">
        <trim prefix="WHERE" prefixOverrides="AND">
            <if test="id != null">AND teacher_pe_criteria.id = #{id}</if>
            <if test="sectionId != null">AND teacher_pe_criteria.section_id = #{sectionId}</if>
            <if test="title != null">AND teacher_pe_criteria.title = #{title}</if>
            <if test="type != null">AND teacher_pe_criteria.type = #{type}</if>
            <if test="points != null">AND teacher_pe_criteria.points = #{points}</if>
            <if test="calculated != null">AND teacher_pe_criteria.calculated = #{calculated}</if>
            <if test="seq != null">AND teacher_pe_criteria.seq = #{seq}</if>
        </trim>
    </sql>
    
    <sql id="teacherPeCriteriaOrder">
        <if test="orderString != null">ORDER BY ${orderString}</if>
    </sql>
    
    <sql id="teacherPeCriteriaLimit">LIMIT #{startLine}, #{limitLine}</sql>
    
    <resultMap type="com.vipkid.trpm.entity.TeacherPeCriteria" id="teacherPeCriteriaResultMap">
        <id column="id" property="id" />
        <result column="section_id" property="sectionId" />
        <result column="title" property="title" />
        <result column="type" property="type" />
        <result column="points" property="points" />
        <result column="calculated" property="calculated" />
        <result column="seq" property="seq" />
    </resultMap>
    
    <select id="findDao" resultMap="teacherPeCriteriaResultMap">
        SELECT
        <include refid="teacherPeCriteriaColumns" />
        FROM teacher_pe_criteria
        <include refid="teacherPeCriteriaWhere" />
    </select>
    
    <select id="countDao" resultType="int">
        SELECT COUNT(*) FROM teacher_pe_criteria
        <include refid="teacherPeCriteriaWhere" />
    </select>

    <select id="pageDao" resultMap="teacherPeCriteriaResultMap">
        SELECT
        <include refid="teacherPeCriteriaColumns" />
        FROM teacher_pe_criteria
        <include refid="teacherPeCriteriaWhere" />
        <include refid="teacherPeCriteriaOrder" />
        <include refid="teacherPeCriteriaLimit" />
    </select>

    <select id="limitDao" resultMap="teacherPeCriteriaResultMap">
        SELECT
        <include refid="teacherPeCriteriaColumns" />
        FROM teacher_pe_criteria
        <include refid="teacherPeCriteriaWhere" />
        <include refid="teacherPeCriteriaOrder" />
        <include refid="teacherPeCriteriaLimit" />
    </select>

    <insert id="saveDao" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO teacher_pe_criteria
        <trim prefix="(" prefixOverrides=",">
            <if test="id != null">,id</if>
            <if test="sectionId != null">,section_id</if>
            <if test="title != null">,title</if>
            <if test="type != null">,type</if>
            <if test="points != null">,points</if>
            <if test="calculated != null">,calculated</if>
            <if test="seq != null">,seq</if>
        </trim>
        ) VALUES
        <trim prefix="(" prefixOverrides=",">
            <if test="id != null">,#{id}</if>
            <if test="sectionId != null">,#{sectionId}</if>
            <if test="title != null">,#{title}</if>
            <if test="type != null">,#{type}</if>
            <if test="points != null">,#{points}</if>
            <if test="calculated != null">,#{calculated}</if>
            <if test="seq != null">,#{seq}</if>
        </trim>
        )
    </insert>

    <update id="updateDao">
        UPDATE teacher_pe_criteria
        <trim prefix="SET" prefixOverrides=",">
            <if test="sectionId != null">,section_id = #{sectionId}</if>
            <if test="title != null">,title = #{title}</if>
            <if test="type != null">,type = #{type}</if>
            <if test="points != null">,points = #{points}</if>
            <if test="calculated != null">,calculated = #{calculated}</if>
            <if test="seq != null">,seq = #{seq}</if>
        </trim>
        <trim prefix="WHERE" prefixOverrides="AND">AND id = #{id}</trim>
    </update>

    <delete id="deleteDao">
        DELETE FROM teacher_pe_criteria
        <trim prefix="WHERE" prefixOverrides="AND">AND id = #{id}</trim>
    </delete>
    
</mapper>
