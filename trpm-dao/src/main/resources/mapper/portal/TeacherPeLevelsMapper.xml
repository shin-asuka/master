<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="TeacherPeLevelsMapper">
    
    <sql id="teacherPeLevelsColumns">teacher_pe_levels.id AS id, teacher_pe_levels.application_id AS application_id, teacher_pe_levels.level AS level</sql>
    
    <sql id="teacherPeLevelsWhere">
        <trim prefix="WHERE" prefixOverrides="AND">
            <if test="id != 0">AND teacher_pe_levels.id = #{id}</if>
            <if test="applicationId != 0">AND teacher_pe_levels.application_id = #{applicationId}</if>
            <if test="level != 0">AND teacher_pe_levels.level = #{level}</if>
        </trim>
    </sql>
    
    <sql id="teacherPeLevelsOrder">
        <if test="orderString != null">ORDER BY ${orderString}</if>
    </sql>
    
    <sql id="teacherPeLevelsLimit">LIMIT #{startLine}, #{limitLine}</sql>
    
    <resultMap type="com.vipkid.trpm.entity.TeacherPeLevels" id="teacherPeLevelsResultMap">
        <id column="id" property="id" />
        <result column="application_id" property="applicationId" />
        <result column="level" property="level" />
    </resultMap>
    
    <select id="findDao" resultMap="teacherPeLevelsResultMap">
        SELECT
        <include refid="teacherPeLevelsColumns" />
        FROM teacher_pe_levels
        <include refid="teacherPeLevelsWhere" />
    </select>
    
    <select id="countDao" resultType="int">
        SELECT COUNT(*) FROM teacher_pe_levels
        <include refid="teacherPeLevelsWhere" />
    </select>
    
    <select id="pageDao" resultMap="teacherPeLevelsResultMap">
        SELECT
        <include refid="teacherPeLevelsColumns" />
        FROM teacher_pe_levels
        <include refid="teacherPeLevelsWhere" />
        <include refid="teacherPeLevelsOrder" />
        <include refid="teacherPeLevelsLimit" />
    </select>
    
    <select id="limitDao" resultMap="teacherPeLevelsResultMap">
        SELECT
        <include refid="teacherPeLevelsColumns" />
        FROM teacher_pe_levels
        <include refid="teacherPeLevelsWhere" />
        <include refid="teacherPeLevelsOrder" />
        <include refid="teacherPeLevelsLimit" />
    </select>
    
    <insert id="saveDao" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO teacher_pe_levels
        <trim prefix="(" prefixOverrides=",">
            <if test="id != 0">,id</if>
            <if test="applicationId != 0">,application_id</if>
            <if test="level != 0">,level</if>
        </trim>
        ) VALUES
        <trim prefix="(" prefixOverrides=",">
            <if test="id != 0">,#{id}</if>
            <if test="applicationId != 0">,#{applicationId}</if>
            <if test="level != 0">,#{level}</if>
        </trim>
        )
    </insert>
    
    <update id="updateDao">
        UPDATE teacher_pe_levels
        <trim prefix="SET" prefixOverrides=",">
            <if test="id != 0">,id = #{id}</if>
            <if test="applicationId != 0">,application_id = #{applicationId}</if>
            <if test="level != 0">,level = #{level}</if>
        </trim>
        <trim prefix="WHERE" prefixOverrides="AND">AND id = #{id}</trim>
    </update>
    
    <delete id="deleteDao">
        DELETE FROM teacher_pe_levels
        <trim prefix="WHERE" prefixOverrides="AND">AND id = #{id}</trim>
    </delete>

    <delete id="deleteByApplicationId">
        DELETE FROM teacher_pe_levels WHERE application_id = #{applicationId}
    </delete>
    
</mapper>
