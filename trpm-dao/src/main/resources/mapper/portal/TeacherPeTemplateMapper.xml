<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="TeacherPeTemplateMapper">
    
    <sql id="teacherPeTemplateColumns">teacher_pe_template.id AS id, teacher_pe_template.name AS name, teacher_pe_template.current AS current</sql>
    
    <sql id="teacherPeTemplateWhere">
        <trim prefix="WHERE" prefixOverrides="AND">
            <if test="id != null">AND teacher_pe_template.id = #{id}</if>
            <if test="name != null">AND teacher_pe_template.name = #{name}</if>
            <if test="current != null">AND teacher_pe_template.current = #{current}</if>
        </trim>
    </sql>
    
    <sql id="teacherPeTemplateOrder">
        <if test="orderString != null">ORDER BY ${orderString}</if>
    </sql>
    
    <sql id="teacherPeTemplateLimit">LIMIT #{startLine}, #{limitLine}</sql>
    
    <resultMap type="com.vipkid.trpm.entity.TeacherPeTemplate" id="teacherPeTemplateResultMap">
        <id column="id" property="id" />
        <result column="name" property="name" />
        <result column="current" property="current" />
    </resultMap>
    
    <select id="findDao" resultMap="teacherPeTemplateResultMap">
        SELECT
        <include refid="teacherPeTemplateColumns" />
        FROM teacher_pe_template
        <include refid="teacherPeTemplateWhere" />
    </select>
    
    <select id="countDao" resultType="int">
        SELECT COUNT(*) FROM teacher_pe_template
        <include refid="teacherPeTemplateWhere" />
    </select>
    
    <select id="pageDao" resultMap="teacherPeTemplateResultMap">
        SELECT
        <include refid="teacherPeTemplateColumns" />
        FROM teacher_pe_template
        <include refid="teacherPeTemplateWhere" />
        <include refid="teacherPeTemplateOrder" />
        <include refid="teacherPeTemplateLimit" />
    </select>
    
    <select id="limitDao" resultMap="teacherPeTemplateResultMap">
        SELECT
        <include refid="teacherPeTemplateColumns" />
        FROM teacher_pe_template
        <include refid="teacherPeTemplateWhere" />
        <include refid="teacherPeTemplateOrder" />
        <include refid="teacherPeTemplateLimit" />
    </select>
    
    <insert id="saveDao" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO teacher_pe_template
        <trim prefix="(" prefixOverrides=",">
            <if test="id != null">,id</if>
            <if test="name != null">,name</if>
            <if test="current != null">,current</if>
        </trim>
        ) VALUES
        <trim prefix="(" prefixOverrides=",">
            <if test="id != null">,#{id}</if>
            <if test="name != null">,#{name}</if>
            <if test="current != null">,#{current}</if>
        </trim>
        )
    </insert>
    
    <update id="updateDao">
        UPDATE teacher_pe_template
        <trim prefix="SET" prefixOverrides=",">
            <if test="name != null">,name = #{name}</if>
            <if test="current != null">,current = #{current}</if>
        </trim>
        <trim prefix="WHERE" prefixOverrides="AND">AND id = #{id}</trim>
    </update>
    
    <delete id="deleteDao">
        DELETE FROM teacher_pe_template
        <trim prefix="WHERE" prefixOverrides="AND">AND id = #{id}</trim>
    </delete>
    
</mapper>
