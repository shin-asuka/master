<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="TeacherPeTagsMapper">
    
    <sql id="teacherPeTagsColumns">teacher_pe_tags.id AS id, teacher_pe_tags.application_id AS application_id, teacher_pe_tags.tag_id AS tag_id</sql>
    
    <sql id="teacherPeTagsWhere">
        <trim prefix="WHERE" prefixOverrides="AND">
            <if test="id != 0">AND teacher_pe_tags.id = #{id}</if>
            <if test="applicationId != 0">AND teacher_pe_tags.application_id = #{applicationId}</if>
            <if test="tagId != 0">AND teacher_pe_tags.tag_id = #{tagId}</if>
        </trim>
    </sql>
    
    <sql id="teacherPeTagsOrder">
        <if test="orderString != null">ORDER BY ${orderString}</if>
    </sql>
    
    <sql id="teacherPeTagsLimit">LIMIT #{startLine}, #{limitLine}</sql>
    
    <resultMap type="com.vipkid.trpm.entity.TeacherPeTags" id="teacherPeTagsResultMap">
        <id column="id" property="id" />
        <result column="application_id" property="applicationId" />
        <result column="tag_id" property="tagId" />
    </resultMap>
    
    <select id="findDao" resultMap="teacherPeTagsResultMap">
        SELECT
        <include refid="teacherPeTagsColumns" />
        FROM teacher_pe_tags
        <include refid="teacherPeTagsWhere" />
    </select>
    
    <select id="countDao" resultType="int">
        SELECT COUNT(*) FROM teacher_pe_tags
        <include refid="teacherPeTagsWhere" />
    </select>
    
    <select id="pageDao" resultMap="teacherPeTagsResultMap">
        SELECT
        <include refid="teacherPeTagsColumns" />
        FROM teacher_pe_tags
        <include refid="teacherPeTagsWhere" />
        <include refid="teacherPeTagsOrder" />
        <include refid="teacherPeTagsLimit" />
    </select>
    
    <select id="limitDao" resultMap="teacherPeTagsResultMap">
        SELECT
        <include refid="teacherPeTagsColumns" />
        FROM teacher_pe_tags
        <include refid="teacherPeTagsWhere" />
        <include refid="teacherPeTagsOrder" />
        <include refid="teacherPeTagsLimit" />
    </select>
    
    <insert id="saveDao" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO teacher_pe_tags
        <trim prefix="(" prefixOverrides=",">
            <if test="id != 0">,id</if>
            <if test="applicationId != 0">,application_id</if>
            <if test="tagId != 0">,tag_id</if>
        </trim>
        ) VALUES
        <trim prefix="(" prefixOverrides=",">
            <if test="id != 0">,#{id}</if>
            <if test="applicationId != 0">,#{applicationId}</if>
            <if test="tagId != 0">,#{tagId}</if>
        </trim>
        )
    </insert>
    
    <update id="updateDao">
        UPDATE teacher_pe_tags
        <trim prefix="SET" prefixOverrides=",">
            <if test="id != 0">,id = #{id}</if>
            <if test="applicationId != 0">,application_id = #{applicationId}</if>
            <if test="tagId != 0">,tag_id = #{tagId}</if>
        </trim>
        <trim prefix="WHERE" prefixOverrides="AND">AND id = #{id}</trim>
    </update>
    
    <delete id="deleteDao">
        DELETE FROM teacher_pe_tags
        <trim prefix="WHERE" prefixOverrides="AND">AND id = #{id}</trim>
    </delete>

    <delete id="deleteByApplicationId">
        DELETE FROM teacher_pe_tags WHERE application_id = #{applicationId}
    </delete>
    
</mapper>
